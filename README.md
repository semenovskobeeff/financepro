# Finance App

Веб-приложение для учёта личных финансов с акцентом на цели накоплений, работу с долгами, кредитами, подписками и расширенной аналитикой.

## 📚 Документация

- **[📖 Руководство по развертыванию](DEPLOYMENT.md)** - детальная инструкция по развертыванию в разных средах
- **[🔄 Переключение режимов](DEPLOYMENT.md#-переключение-между-режимами)** - как переключаться между разработкой и продакшеном

## Основные принципы

- Строгая приватность пользовательских данных
- Минималистичный интерфейс в стиле Notion
- Акцент на мобильной версии (Mobile First)
- Система архивирования вместо физического удаления
- Детальная аналитика финансовых данных
- Напоминания о предстоящих платежах

## Технологический стек

### Frontend

- React 18
- TypeScript
- Redux Toolkit + RTK Query
- Material UI v5
- Chart.js

### Backend

- Node.js 20
- Express
- MongoDB 7
- Mongoose
- JWT

## 🚀 Установка и запуск

### Предварительные требования

Убедитесь, что у вас установлены:

- **Node.js** (версия 18.x или выше) - [Скачать](https://nodejs.org/)
- **npm** (устанавливается с Node.js)
- **Git** - [Скачать](https://git-scm.com/)

### 📥 Шаг 1: Клонирование репозитория

```bash
git clone <url-репозитория>
cd finance-app
```

### 📦 Шаг 2: Установка зависимостей

Выполните следующие команды **в строгом порядке**:

```bash
# 1. Установка зависимостей корневого проекта (включает concurrently и nodemon)
npm install

# 2. Установка зависимостей клиента (React приложение)
cd client
npm install

# 3. Установка зависимостей сервера (Express приложение)
cd ../server
npm install

# 4. Вернемся в корневую папку
cd ..
```

**Альтернативный способ** (установка всех зависимостей одной командой):

```bash
npm run install:all
```

### ⚙️ Шаг 3: Настройка переменных окружения

Для работы с реальной базой данных создайте файл `.env` в папке `server/`:

```bash
# server/.env
NODE_ENV=development
PORT=3001
SEED_DATABASE=true

# MongoDB Atlas (замените на ваши данные)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# JWT Secret (используйте сложный ключ для продакшена)
JWT_SECRET=your-very-long-and-secure-secret-key-here

# Email настройки (опционально, для восстановления пароля)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
```

> **⚠️ Важно**: Файл `.env` добавлен в `.gitignore` и не попадет в репозиторий. Никогда не коммитьте реальные пароли и ключи!

### 🎭 Шаг 4: Выбор режима работы

Приложение поддерживает два режима:

#### Режим 1: Тестовые данные (MSW) 🧪

**Подходит для**: быстрого старта, демонстрации, разработки UI

```bash
# Запуск только клиента с тестовыми данными
cd client
npm run dev
```

- Открыть: http://localhost:5175
- Данные: Автоматически загружаются через Mock Service Worker
- Сервер: Не требуется

#### Режим 2: Реальная база данных 🌐

**Подходит для**: полноценной разработки, тестирования API

```bash
# Вариант A: Запуск обоих сервисов одной командой (из корневой папки)
npm run dev

# Вариант B: Запуск по отдельности
# Терминал 1 - Сервер:
cd server
npm run dev

# Терминал 2 - Клиент:
cd client
npm run dev
```

- Клиент: http://localhost:5175
- Сервер: http://localhost:3001
- API: http://localhost:3001/api

### 🔄 Переключение между режимами

После запуска клиента:

1. В правом верхнем углу найдите панель **"Режим данных"**
2. Нажмите на стрелку для расширения
3. Переключите тумблер между **"Тестовые данные"** и **"Реальный API"**
4. Страница автоматически перезагрузится

### ✅ Проверка работы

#### Клиент (Frontend)

- **URL**: http://localhost:5175
- **Проверка**: Должна открыться страница входа или дашборд

#### Сервер (Backend) - только для режима с реальной БД

- **Статус**: http://localhost:3001/api/health
- **База данных**: http://localhost:3001/api/health/database
- **API документация**: http://localhost:3001/api

### 🔧 Полезные команды

```bash
# Запуск всего приложения
npm run dev

# Запуск только сервера
npm run dev:server

# Запуск только клиента
npm run dev:client

# Сборка продакшн версии
npm run build

# Запуск тестов
npm test

# Проверка процессов на портах (Windows)
check-processes.bat
```

### 🐛 Решение проблем

#### Порт уже занят

```bash
# Windows
netstat -ano | findstr :3001
netstat -ano | findstr :5175
taskkill /f /pid <PID>

# macOS/Linux
lsof -ti:3001 | xargs kill -9
lsof -ti:5175 | xargs kill -9
```

#### Ошибки установки зависимостей

```bash
# Очистка кэша npm
npm cache clean --force

# Удаление node_modules и переустановка
rm -rf node_modules package-lock.json
npm install
```

#### Ошибка подключения к MongoDB

- Проверьте правильность `MONGODB_URI` в `.env`
- Убедитесь, что IP-адрес добавлен в whitelist MongoDB Atlas
- Проверьте логин и пароль пользователя БД

### 🎯 Быстрый старт для разработки

```bash
# 1. Клонирование и установка
git clone <url>
cd finance-app
npm run install:all

# 2. Создание .env файла (см. выше)
# server/.env

# 3. Запуск в режиме тестовых данных (без сервера)
cd client
npm run dev
# Открыть: http://localhost:5175

# 4. Для работы с реальной БД - запуск полного стека
cd ..
npm run dev
# Клиент: http://localhost:5175
# Сервер: http://localhost:3001
```

## Структура проекта

### Frontend (Feature-Sliced Design)

- `/app` - инициализация приложения, маршрутизация, store
- `/processes` - бизнес-процессы приложения
- `/features` - пользовательские фичи
- `/entities` - бизнес-сущности
- `/widgets` - композиция для страниц
- `/shared` - переиспользуемый код

### Backend (Clean Architecture)

- `/core` - ядро приложения, не зависящее от фреймворков
- `/modules` - модули по функциональным доменам

## Система управления задачами

Проект использует Task Master для управления задачами:

```bash
# Показать список задач
./task list

# Обновить статус задачи
./task update <task-id> <status>

# Создать новую задачу
./create-task <task-id> "<название задачи>" [<приоритет>] [<зависимости>]

# Синхронизировать задачи с Git
./git-sync update

# Создать коммит с упоминанием активных задач
./git-sync commit "Сообщение коммита"
```

## Функциональные модули

- Авторизация и управление пользователями
- Счета и операции
- Цели накопления
- Долги, кредиты и кредитные карты
- Подписки
- Категории расходов и источники доходов
- Аналитика и отчёты
- Архив

## Возможности

- ✅ Управление счетами и операциями
- ✅ Управление долгами и кредитами
- ✅ Управление подписками
- ✅ Управление целями накоплений
- ✅ Система категорий
- ✅ Аналитика и отчёты
- ✅ Архивирование данных
- ✅ Дашборд с предстоящими платежами по подпискам и долгам

## Dashboard

На главной странице отображается:

- **Быстрые действия** - добавление доходов, расходов, перевод средств
- **Финансовая статистика** - общий баланс, доходы/расходы за месяц
- **Графики** - распределение финансов и обзор счетов
- **Предстоящие платежи**:
  - Блок "Подписки" - показывает предстоящие списания по подпискам
  - Блок "Долги и кредиты" - показывает предстоящие платежи по долгам

## API Endpoints

### Долги

- `GET /api/debts` - список долгов
- `GET /api/debts/upcoming?days=7` - предстоящие платежи по долгам
- `POST /api/debts` - создать долг
- `PUT /api/debts/:id` - обновить долг
- `POST /api/debts/:id/payment` - внести платеж

### Подписки

- `GET /api/subscriptions` - список подписок
- `GET /api/subscriptions/upcoming?days=7` - предстоящие платежи по подпискам
